<div class="container">
    <div class="home-banner text-center p-5">
        <h1>Cocktails</h1>
    </div>
    <div class="row justify-content-center">
        <% @cocktails.each do |cocktail| %>
        <div class="col-lg-4 col-12  p-2">

            <div class="card" style="height: 100%;">
                <% if cocktail.photos.attached? %>
                <img class="card-img-top" src="<%= cl_image_path cocktail.photos.last.key, height: 300, width: 300%>" alt="Card image cap">
                <%else%>
                <%= image_tag  "glass_water.jpg" , {:style => "width:300px; height:300px; object-fit:cover"}%>
                <%end%>        
                <div class="card-body">
                <div class="card-title text-center">
                    <p><strong> <%= link_to cocktail.name, cocktail_path(cocktail)%></strong></p>
                </div>
                <div class="card-text"><%=cocktail.description%></div>
            </div>
            </div>
        </div>


        <% end %>
    </div>
    

